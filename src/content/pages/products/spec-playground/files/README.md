# Spec playground files

This directory is intentionally empty for the demo product.

Use `product.manifest.json` as the upload/intake SSOT. For production digital products, deliver files after purchase through Cloudflare R2 plus a Worker/API layer, not by linking raw files from the static page.
