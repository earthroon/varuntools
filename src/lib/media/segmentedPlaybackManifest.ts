export type SegmentStreamManifest={schema:'vacms-playback-segment-stream@1';streamId:string;assetId:string;sourceHash:string;profile:'VACMS-MOBILE-ECONOMY-V1';durationUs:number;segmentPolicy:{targetDurationUs:number;maxDurationUs:number;videoSyncStartRequired:true};video:{codec:string;init:{path:string;sizeBytes:number;sha256:string}};audio:null|{codec:string;init:{path:string;sizeBytes:number;sha256:string}};segments:Array<{ordinal:number;startUs:number;durationUs:number;video:{path:string;sizeBytes:number;sha256:string;syncStart:true};audio:null|{path:string;sizeBytes:number;sha256:string}}>}
export function assertManifest(value:any):SegmentStreamManifest{if(!value||value.schema!=='vacms-playback-segment-stream@1'||value.profile!=='VACMS-MOBILE-ECONOMY-V1'||!Array.isArray(value.segments)||!value.video?.init?.path)throw new Error('E_PUBLIC_SEGMENT_MANIFEST_INVALID');for(const s of value.segments){if(!Number.isSafeInteger(s.ordinal)||s.ordinal<0||!Number.isFinite(s.startUs)||!Number.isFinite(s.durationUs)||s.durationUs<=0||s.video?.syncStart!==true||!s.video?.path)throw new Error('E_PUBLIC_SEGMENT_MANIFEST_ENTRY_INVALID');if((Number(s.video.sizeBytes)||0)+(Number(s.audio?.sizeBytes)||0)>512*1024)throw new Error('E_PUBLIC_SEGMENT_MANIFEST_BUDGET_INVALID')}return value as SegmentStreamManifest}
