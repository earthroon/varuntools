# Commit 116 - Inquiry Worker API Contract

This commit introduces the Worker API contract before persistence.

Worker-first delivery becomes the default submission path, while Commit 120 keeps the Worker-first rollout state explicit for later operator review.
