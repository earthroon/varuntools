# Commit 78 Migration Note

Adds Store Admin Ops Pack / Sales & Grant Ledger. This commit is read-only. It adds no revoke, reissue, refund, webhook replay, INSERT, UPDATE, or DELETE action. Verify with `npm run smoke:ops-ledger`.
