#!/usr/bin/env node
import fs from 'node:fs'
import path from 'node:path'
import process from 'node:process'
const root=process.cwd(); const read=(f)=>fs.readFileSync(path.join(root,f),'utf8'); const exists=(f)=>fs.existsSync(path.join(root,f)); const failures=[];
const required=['docs/ops/sales-ledger.md','docs/ops/grant-ledger.md','docs/ops/refund-revoke-playbook.md','docs/ops/grant-reissue-playbook.md','docs/ops/r2-delivery-incident-playbook.md','docs/ops/support-escalation-playbook.md','docs/ops/ops-status-registry.md','docs/ops/export-contract.md','admin/src/lib/opsStatus.ts','admin/src/lib/opsExportColumns.ts','admin/src/views/OpsOverviewView.vue','admin/src/views/OpsPlaybooksView.vue','docs/migration/commit-78.md'];
for(const f of required) if(!exists(f)) failures.push(`missing required file: ${f}`);
const tokens=[['admin/src/lib/opsStatus.ts','paid'],['admin/src/lib/opsStatus.ts','grant-created'],['admin/src/lib/opsStatus.ts','claim-opened'],['admin/src/lib/opsStatus.ts','downloaded'],['admin/src/lib/opsStatus.ts','expired'],['admin/src/lib/opsStatus.ts','refunded'],['admin/src/lib/opsStatus.ts','revoked'],['admin/src/lib/opsStatus.ts','support-needed'],['admin/src/lib/opsStatus.ts','delivery-failed'],['admin/src/lib/opsStatus.ts','webhook-failed'],['admin/src/lib/opsStatus.ts','reissue-needed'],['docs/ops/sales-ledger.md','sales ledger'],['docs/ops/grant-ledger.md','grant ledger'],['docs/ops/refund-revoke-playbook.md','refund'],['docs/ops/refund-revoke-playbook.md','revoke'],['docs/ops/grant-reissue-playbook.md','reissue'],['docs/ops/r2-delivery-incident-playbook.md','R2_OBJECT_MISSING'],['admin/src/views/OpsOverviewView.vue','Read-only ops view'],['admin/src/views/OpsPlaybooksView.vue','Manual review required']];
for(const [f,t] of tokens) if(!exists(f)||!read(f).includes(t)) failures.push(`${f} missing token: ${t}`);
const pkg=JSON.parse(read('package.json')); if(!pkg.scripts?.['smoke:ops-ledger']) failures.push('package.json missing smoke:ops-ledger'); if(!read('scripts/check-launch.mjs').includes('smoke-ops-ledger.mjs')) failures.push('check-launch missing smoke-ops-ledger'); if(!read('admin/src/router.ts').includes("'/ops'")||!read('admin/src/router.ts').includes("'/playbooks'")) failures.push('admin router missing /ops or /playbooks');
function walk(d){ if(!fs.existsSync(d)) return []; return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]) }
for(const f of walk(path.join(root,'workers/admin-api/src')).filter(x=>/\.(ts|js)$/.test(x))){ const rel=path.relative(root,f); const text=fs.readFileSync(f,'utf8'); for(const pattern of [/prepare\([^)]*UPDATE\s/i,/prepare\([^)]*INSERT\s/i,/prepare\([^)]*DELETE\s/i]) if(pattern.test(text)) failures.push(`${rel} contains forbidden runtime mutation pattern: ${pattern}`) }
for(const f of walk(path.join(root,'admin/src')).filter(x=>/\.(ts|vue)$/.test(x))){ const rel=path.relative(root,f); const text=fs.readFileSync(f,'utf8'); for(const token of ['@click="revoke','@click="reissue','@click="refund','revokeGrant(','reissueGrant(','refundOrder(','Revoke Now','Reissue Now','Refund Now','Replay Now']) if(text.includes(token)) failures.push(`${rel} contains forbidden write UI token: ${token}`) }
const exportContract=read('admin/src/lib/opsExportColumns.ts'); for(const token of ['rawJson','r2Key','privatePath','downloadUrl','publicUrl']) if(!exportContract.includes(token)) failures.push(`opsExportColumns must explicitly forbid ${token}`);
if(failures.length){ console.error('[smoke:ops-ledger] FAILED'); failures.forEach(f=>console.error('- '+f)); process.exit(1) } console.log('[smoke:ops-ledger] OK ops ledger docs, registry, export contract, and read-only boundary are sealed')
